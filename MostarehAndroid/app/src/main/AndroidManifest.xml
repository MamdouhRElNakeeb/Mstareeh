<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ibrandcity.mstareeh">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        android:name=".helper.MstareehApp"
        tools:replace="android:supportsRtl">
        <activity android:name=".activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.Register"/>
        <activity android:name=".activity.Home"/>
        <activity android:name=".activity.BuildingOrder"/>
        <activity android:name=".activity.BuildingForm"/>
        <activity android:name=".activity.BuildingEvaluation"/>
        <activity android:name=".activity.AppEvaluation"/>
    </application>

</manifest>